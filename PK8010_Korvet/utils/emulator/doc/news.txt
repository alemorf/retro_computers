более старые новости канули в лету .....

2003-06-21
	1) Клавиатура, добавил переключение раскладок через GUI
	2) GUI собстно добавлен переключатель QWERTY/JCUKEN, исправлен баг с BWFlag
	   неверно отображался если переключать то кнопкой, то GUI.
	3) screen.c
	     оптимизация ACZU, 
             добавлен масив флагов изменения байта, и соотв обновляем не весь ACZUPlane 
             а только необходимые символы.
             ACZU_Inv - теперь напрямую используется, val=c ^ ACZUInv, а не val=c ^ ACZUInv?0xff:0
             почистил, удавил EGA
             добавил ifdef для SLOWBITMAPCONVERT
        4) _main.c
           небольшая чистка. EGA, load бинарников.

2003-06-28
	1) screen.c
	     GZU_Read Color Mode
             заново придумал чтение в режиме цвета  ;-(((
             первоначально забыл инверитровать результат.
             сегодня придумал заново, полез глянул в старый исходник и обнаружил ...
             мда, вместо ^FF  навоял такого монстра ... Ж-(

             в итоге получилось 
		if (!(NCREG & RSEL0)) C0^=0xff;
		if (!(NCREG & RSEL1)) C1^=0xff;
		if (!(NCREG & RSEL2)) C2^=0xff;
		Value=(C0&C1&C2)^0xff;

             работает раза в 4 быстрее чем дурной вариант, а места занимает ...
2003-07-03
	1)  FLOPPY.C & _main.c
		На экране отображается обращение к дисководу и текущая дорожка.
	2)  memory.c
		небольшая оптимизация доступа к памяти.

2003-07-25
	1) Переделал экранные лампочки, теперь приличный внешний вид.
            FPS отображается в левом угду на красном фоне.
            FDD led, отображается активность дисковода и текущая дорожка.
            LUT значение таблицы лут, цвет кубика - реальный цвет, тот что на экране, полоска - оригинальный
            за шрифт и идею Special Thanks to WinUAE team.

        2) CPU, ускорил на 5-10%, просто убрал вызов функции execute, вставил прямо в тело.

        3) Printer Support ;-)
           в текущем катологе создается файл printer.txt и в него пишем все чего печатается на принтер.

2003-07-30
        1) если FPS >100, то 200 - A, 300 - B, 400 - C, ... , >=7 - F
        2) есть странный глюк, экран переодически очищается,
           приделал dirty fix, каждый фрейм проверяем точку на экране, если ее цвет отличается
           от нужного то перерисовать рямочку.
           рямочка стала фигурной ;-).
        3) появилась возможность выбрать размер видеопамяти, в конфиге параметр GZU_Pages
           по умолчанию 4 страницы, можно указать 1 для эмуляции первого корвета.
        4) в конфиге 3 параметра
             OSD_FPS  - отображать индикатор FPS
             OSD_FDD  -      -//-            Дисков 
             OSD_LUT  -      -//-            таблицы LUT
2003-08-01
        1) FPS теперь 4 знака >1000, то 2000 - A, 3000 - B, 4000 - C, ... , >=7 - F
        2) Попробуем делать анализ PIC не каждый такт, а только каждый FRAME
           из синхронных прерывания - только 50hz, остальные - асинхронные, и их со 
           спокойной совестью можно привязать к фрейму.
           производительность выросла немного ;-)

2004-05-14
	1) исправил СТАРЫЙ глюк, при установке режима scr_Attr_Write в 3 (Store)
           атрибут не копировался, а просто игнорировался.
	   выглядело как при скролле экрана инверсные атрибуты не копировались.
           (например заставка "альтернативы" и менюшка ExptressPascal оставались на месте)
           причем в GZU_Read атрибут сохранял, а в GZU_Write забыл его использовать ....

2004-09-11	
	1) DBG:GT Исправлена реакция на ESC, теперь просто выходим а не повторяем пред. действие
	2) GUI	  Вызывается LeftALT+F9 и в DBG и в обычной версии, пофиксил менюхи под 8x16 фонт

2004-10-01
	1) _MAIN,KEYBOARD,OSD
 	   Сделал режим клавиатуры AUTO, в котором JCUKEN/QWERTY  переключаеться автоматом
           в соответствии с флагои рус/лат и нажатием клавиши АЛФ (Left ALF)
	2) Исправил JCUKEN расскладку клавиатуры, были нажатия сразу нескольких клавиш.
	3) Эмулятор теперь умеет работать в 3х режимах 
           а)  640x480 FullScreen  - Режим по умолчанию, только экран корвета
           б)  640x480 Windowed    - оконный режим.
           в) 1024x768 FullScreen  - экран эмулятора с включенным отладчиком.

           стартуем в режмие определяемом в конфиг файле переменной WINDOWED, если не равна 0 то 
           в оконном. В эмуляторе переключаем по клавише F8 (windowed <> fullscreen)
           при нажатии F9 (вход в отладчик) включаем режим 1024x768 FullScreen.
           при необходимости можно нажав F8 вернуться к 640x480.

           дамп памяти перенесен на Alt+F8

        4) Починил F6 - FullSpeed button

2004-10-11
	1) Приделал на экран в не DBG режиме маленький хелп. сдвинул екран.
        2) в конфиге KEYBOARD_MODE - Режим работы клавиатуры, 0 - QWERTY, 1 - JCUKEN, 2 - AUTO
 