;+---------------------------------------------------------------------------
; MXOS
; Вывод символа на экран (или принтер)
;
; На входе
;  с - символ
;
; На выходе
;  Все регистры сохраняются
;
; 2013-12-12 Дизассемблировано vinxru
;----------------------------------------------------------------------------

printChar:	push	psw
		push	d
		push	b
		push	h
		call	printChar2
		pop	h
		pop	b
		pop	d
		pop	psw
		ret
