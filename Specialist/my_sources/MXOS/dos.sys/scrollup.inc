;+---------------------------------------------------------------------------
; MXOS
; Прокрутка экрана вверх
;
; На выходе
;   af, bc - сохраняются
;
; 2013-12-12 Дизассемблировано vinxru
;----------------------------------------------------------------------------

		; Сохраняем регистры
		push	b
		push	psw

		; Сохраняем SP
		lxi	h, 0
		dad	sp
		shld	v_oldSP

		; Прокрутка экрана вверх
		lxi	sp, 900Ah	; Откуда
		mvi	h, 90h		; Куда
		mvi	b, 48		; Столбцов
		jmp	scrollUp2