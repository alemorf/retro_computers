;+---------------------------------------------------------------------------
; RAMFOS
; Стандартная точка входа F842
; Установка адреса загрузки в память текущего файла
;
; На входе
;   v_header2_name - имя файла
;   hl             - новый адрес
;
; 2013-11-01 Дизассемблировано vinxru
;----------------------------------------------------------------------------

fileSetAddr:	; Ищем файл
		push	h
		 call	fileFind
		pop	h

		; Файл не найден
		rnz

		; Устаналиваем адрес загрузки
		jmp	fileLoadSetAddr
