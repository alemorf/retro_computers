;+---------------------------------------------------------------------------
; RAMFOS
; Стандартная точка входа F82D
; Установить расширение сохраняемого/загружаемого файла
;
; 2013-11-01 Дизассемблировано vinxru
;----------------------------------------------------------------------------

fileSetType:	push	h
		push	d
		push	b
		 lxi	d, v_header2_ext
		 mvi	c, 2	; Размер -1
memcpy_popa:	 push	psw
		  call	memcpy_de_hl_c
		 pop	psw
		pop	b
		pop	d
		pop	h
		ret