;+---------------------------------------------------------------------------
; RAMFOS
; Ожидание нажатия клавиши без курсора
;
; 2013-11-01 Дизассемблировано vinxru
;----------------------------------------------------------------------------

; Тут ошибка, курсор всётаки может промелькнуть

keyWaitNoCursor:call	j_keyCheck
		jnz	j_keyWait
		jmp	keyWaitNoCursor