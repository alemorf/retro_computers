;+---------------------------------------------------------------------------
; RAMFOS
; Копирование ОЗУ
;
; На входе
;  de - откуда
;  hl - куда
;  с  - размер / 2
; 
; 2013-11-01 Дизассемблировано vinxru
;----------------------------------------------------------------------------

memcpy_hl_de_c2:ldax	d
		mov	m, a
		inr	l
		inr	e
		ldax	d
		mov	m, a
		inr	l
		inr	e
		dcr	c
		jnz	memcpy_hl_de_c2
		ret
