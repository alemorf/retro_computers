;+---------------------------------------------------------------------------
; RAMFOS
; Копирование ОЗУ в обратном направлении
;
; На входе
;  de - откуда
;  hl - куда
;  с  - размер/2
; 
; 2013-11-01 Дизассемблировано vinxru
;----------------------------------------------------------------------------

memcpyb_hl_de_c2:
		dcr	l
		dcr	e
		ldax	d
		mov	m, a

		dcr	l
		dcr	e
		ldax	d
		mov	m, a

		dcr	c
		jnz	memcpyb_hl_de_c2
		ret
