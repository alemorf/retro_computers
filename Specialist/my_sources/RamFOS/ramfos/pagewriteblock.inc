;+---------------------------------------------------------------------------
; RAMFOS
; Запись блока данных в ДОЗУ/ПЗУ
;
; На входе:
;  bc - откуда, адрес в памяти
;  hl - куда, адрес в ДОЗУ/ПЗУ
;  de - длина-1
;
; 2013-11-01 Дизассемблировано vinxru
;----------------------------------------------------------------------------

pageWriteBlock:	; Чтение байта
		ldax	b
		; Запись байта
		push	b
		 mov	c, a
		 call	pageWrite
		pop	b
		; Увеличение адресов
		inx	h
		inx	b
		; if(de == 0) break;
		mov	a, e
		ora	d
		; de--
		dcx	d
		jnz	pageWriteBlock
		ret
